---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awdns_domain Data Source - awdns"
subcategory: ""
description: |-
  Look up a single Authentic Web DNS domain by its numeric id or its fqdn. Exactly one of the two must be set.
---

# awdns_domain (Data Source)

Look up a single Authentic Web DNS domain by its numeric `id` or its `fqdn`. Exactly one of the two must be set.

## Example Usage

```terraform
terraform {
  required_providers {
    awdns = {
      source = "AuthenticWeb/awdns"
    }
  }
}

variable "awdns_client_id" {
  type = string
}

variable "awdns_client_secret" {
  type      = string
  sensitive = true
}

provider "awdns" {
  client_id     = var.awdns_client_id
  client_secret = var.awdns_client_secret
}

# Look up a single domain by its fully-qualified name...
data "awdns_domain" "by_fqdn" {
  fqdn = "example.com"
}

# ...or by its numeric id (supply exactly one of id / fqdn).
data "awdns_domain" "by_id" {
  id = "42"
}

output "example_status" {
  value = data.awdns_domain.by_fqdn.status
}

output "example_nameservers" {
  value = data.awdns_domain.by_fqdn.nameserver_list
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `fqdn` (String) Fully-qualified domain name. Set this OR `id` (not both). Computed when looked up by `id`.
- `id` (String) Numeric domain ID. Set this OR `fqdn` (not both). Computed when looked up by `fqdn`.

### Read-Only

- `dns_provider` (String) DNS provider backing the zone.
- `name` (String) Domain name as registered upstream.
- `nameserver_list` (List of String) Authoritative nameservers for the domain.
- `status` (Number) Upstream domain status code (e.g. 7 = active, 9 = expired). See the awdns-go DomainStatus* constants.
- `tld` (String) Top-level domain.
- `use_our_zone` (Boolean) Whether the domain uses an Authentic-Web-hosted zone.
